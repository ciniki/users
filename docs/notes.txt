**Permissions**

The user information can be updated by a tenant if the user 
is not connected with any other tenants via the 
ciniki_tenant_users table.  

A user can be removed from a tenant by removing them from the
ciniki_tenant_users table for that tenant.
